/**
 * Wheels component index
 *
 * @format
 * @flow
 */

import SpanButton from "./SpanButton";
import CircleButton from "./CircleButton";

const wheels = {SpanButton, CircleButton};

export default wheels;
